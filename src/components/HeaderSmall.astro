---
import ImageComp from "./ImageComp.astro";

const { content } = Astro.props;
---

<section
  class="relative lg:mt-0 min-h-[75vh] font-sans text-white flex flex-col items-center justify-center overflow-hidden pt-12 pb-6"
>
  <ImageComp
    imagePath={content.imagePath}
    imageAlt={content.imageAlt}
    styles="absolute inset-0 w-full h-full object-cover"
    quality={80}
    loading="eager"
    className="z-0"
  />
  <div
    class="absolute inset-0 w-full bg-muted-950/70 h-full z-0"
  >
  </div>
  <!-- Text -->
  <div
    class="text-white mt-20 lg:mt-0 text-center lg:text-left z-10 flex flex-col items-center px-5 h-full lg:pt-28 justify-center animate-fade-top"
  >
    <h1
      class="text-5xl md:text-6xl lg:text-8xl font-title drop-shadow-lg xl:text-8xl text-center font-bold animate-fade-top"
      set:html={content.headerH1}
    />
    <div
      class="mt-10 flex flex-wrap justify-center align-middle items-center gap-3 z-10"
    >
      {
        content.buttons &&
          content.buttons.map((btn) => (
            <a
              href={btn.link}
              class={`${btn.primary ? "btn-primary" : "btn-white"} w-full md:w-auto`}
            >
              <span class="drop-shadow-sm">{btn.name}</span>
            </a>
          ))
      }
    </div>
  </div>
</section>
